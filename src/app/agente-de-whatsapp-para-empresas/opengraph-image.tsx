/* Ver el comentario en `proceso/opengraph-image.tsx`: sin este archivo la ruta
   se comparte sin imagen, y no se nota hasta que alguien pega el enlace. */
export { default, alt, size, contentType } from "../opengraph-image";
